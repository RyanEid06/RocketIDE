You are implementing RocketIDE from this repository.

Before touching code, read these files completely:
1. GEMINI.md
2. docs/ROCKET_IDE_DESIGN_SPEC.md
3. docs/ROCKET_INTEGRATION_CONTRACT.md
4. ROCKET_IDE_IMPLEMENTATION_PLAN.md
5. the relevant files under references/rocket-current/

Then inspect the current repository state and start with the FIRST work package in ROCKET_IDE_IMPLEMENTATION_PLAN.md whose status is READY and whose dependencies are satisfied.

Rules:
- Work on ONE WP at a time.
- Do not jump ahead to visually exciting features.
- Use tests as required by the WP.
- Never implement a second Rocket parser/type checker/formatter/semantic engine.
- Never fake compiler/LSP behavior.
- Do not mark a WP DONE until its acceptance criteria, scripts/verify.ps1, and required Windows CI evidence pass.
- After successful completion, update the Progress Ledger and append the exact completion evidence block described in the plan.
- If Rocket itself lacks a required capability, record a ROCKET-UPSTREAM-REQUEST instead of creating incompatible IDE-only language semantics.
- Keep the repository clean and commit the WP with the commit message requested by the plan or a clearly equivalent conventional commit.

Start by auditing IDE-WP00 against the files already provided. Repair only what is needed to make WP00 genuinely pass. Do not start IDE-WP01 in the same pass unless WP00 has been verified, committed, pushed, and Windows CI is green.
