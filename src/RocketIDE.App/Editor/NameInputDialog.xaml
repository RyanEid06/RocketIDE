<Window x:Class="RocketIDE.App.Editor.NameInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="420" Height="160" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptText" TextWrapping="Wrap" />
        <TextBox x:Name="ValueTextBox" Grid.Row="1" Margin="0,10,0,0" MinWidth="320" KeyDown="ValueTextBox_KeyDown" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Width="80" Margin="0,12,8,0" Content="Cancel" IsCancel="True" />
            <Button Width="80" Margin="0,12,0,0" Content="OK" IsDefault="True" Click="Ok_Click" />
        </StackPanel>
    </Grid>
</Window>
