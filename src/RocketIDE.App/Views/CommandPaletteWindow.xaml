<Window x:Class="RocketIDE.App.Views.CommandPaletteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Command Palette"
        Width="660" Height="440" MinWidth="480" MinHeight="300"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{DynamicResource IDE.WindowBrush}"
        PreviewKeyDown="Window_PreviewKeyDown">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBox x:Name="QueryBox" Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}"
                 FontSize="15" Padding="8,6" />
        <ListBox x:Name="CommandList" Grid.Row="1" Margin="0,8,0,6"
                 ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                 MouseDoubleClick="CommandList_MouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="4,3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="{Binding DisplayName}" Foreground="{DynamicResource IDE.TextBrush}" />
                            <TextBlock Text="{Binding Category}" FontSize="11" Foreground="{DynamicResource IDE.TextMutedBrush}" />
                        </StackPanel>
                        <TextBlock Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center"
                                   Text="{Binding Gesture}" Foreground="{DynamicResource IDE.TextMutedBrush}" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style TargetType="{x:Type ListBoxItem}" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                    <Setter Property="IsEnabled" Value="{Binding IsEnabled}" />
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
        <TextBlock Grid.Row="2" Text="{Binding StatusText}" Foreground="{DynamicResource IDE.TextMutedBrush}" />
    </Grid>
</Window>
