<Window x:Class="RocketIDE.App.Views.SymbolPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Go to Symbol" Width="700" Height="480" MinWidth="500" MinHeight="320"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{DynamicResource IDE.WindowBrush}"
        PreviewKeyDown="Window_PreviewKeyDown">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBox x:Name="QueryBox" Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}" Padding="8,6" FontSize="15" />
        <ListBox x:Name="SymbolList" Grid.Row="1" Margin="0,8,0,6"
                 ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                 MouseDoubleClick="SymbolList_MouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="4,3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="90" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding KindText}" Foreground="{DynamicResource IDE.TextMutedBrush}" />
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="{Binding Name}" Foreground="{DynamicResource IDE.TextBrush}" />
                            <TextBlock Text="{Binding Detail}" FontSize="11" Foreground="{DynamicResource IDE.TextMutedBrush}" TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <TextBlock Grid.Row="2" Text="{Binding StatusText}" Foreground="{DynamicResource IDE.TextMutedBrush}" />
    </Grid>
</Window>
